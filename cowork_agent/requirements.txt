# ============================================
# Cowork Agent — Unified Requirements
# ============================================
# Install everything:  pip install -r requirements.txt
# ============================================

# ── Core (required) ──────────────────────────
pyyaml>=6.0
httpx>=0.25.0

# ── LLM Providers ────────────────────────────
openai>=1.0
anthropic>=0.25

# ── Web Tools (search & fetch) ───────────────
trafilatura>=1.6
markdownify>=0.11
beautifulsoup4>=4.12
lxml>=4.9

# ── API & Dashboard ──────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0

# ── Remote Control Bots ──────────────────────
python-telegram-bot>=21.0
slack-bolt>=1.18.0

# ── Skill: PPTX (presentations) ─────────────
python-pptx>=0.6.21
Pillow>=10.0
requests>=2.31

# ── Skill: XLSX (spreadsheets) ───────────────
openpyxl>=3.1.2
pandas>=2.0

# ── Skill: PDF ───────────────────────────────
pdfplumber>=0.10
pypdf>=3.15
reportlab>=4.0

# ── Skill: DOCX (Word documents) ────────────
python-docx>=0.8.11

# ── Security (encryption) ────────────────────
cryptography>=41.0

# ── Persistent Storage ───────────────────────
# sqlite3 is included in Python stdlib — no extra install needed

# ── Browser Automation (optional) ────────────
# playwright>=1.40    # Run: playwright install chromium

# ── OCR (optional — for scanned PDFs) ────────
# pytesseract>=0.3.10
# Requires system package: tesseract-ocr
